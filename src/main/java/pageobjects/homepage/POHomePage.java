package pageobjects.homepage;

import org.openqa.selenium.WebDriver;
import pageobjects.POBase;

public class POHomePage extends POBase {

    public POHomePage(WebDriver driver) {
        super(driver);
    }

    //TODO is this used somewhere?
}
